// import { store } from '../state/root-store';
// import axios from 'axios';
// // import * as qs from 'qs';

// export default {
//   upload: (file: File, name: string) => {
//     const data = new FormData();
//     data.append('file', file);
//     data.append('name', name);
//     return axios.post(`${store.getState().config.serverUrl}/files`, data);
//   }
//   // buildUrl: (id: string, args?: any) => { // ImageProcessArgs
//   //   const argsQuery = args ? `?${qs.stringify(args)}` : '';
//   //   return `${store.getState().config.serverUrl}/file/${id}${argsQuery}`;
//   // }
// };
